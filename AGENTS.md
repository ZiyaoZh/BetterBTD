# BetterBTD Agent Guide

本文件适用于仓库根目录及其所有子目录。若更深层目录存在单独的 `AGENTS.md`，以更深层文件的规则为补充；发生冲突时，用户的显式要求始终优先。

## 项目概况

- BetterBTD 是面向《气球塔防 6》的 Windows x64 WPF 桌面应用。
- 主项目目标框架为 `net8.0-windows10.0.22621.0`，使用 WPF、WPF UI、CommunityToolkit.Mvvm、OpenCvSharp 和 Windows 平台 API。
- `BetterBTD/` 包含主程序，`BetterBTD.Tests/` 包含 xUnit 测试。
- `Fischless.GameCapture/`、`Fischless.HotkeyCapture/` 和 `Fischless.WindowsInput/` 是平台基础库。
- 架构、构建和协议约定以 `docs/developer/` 下的文档为准。

## 工作原则

- 修改前先阅读相关实现、测试和开发者文档，遵循现有命名、分层和代码风格。
- 保持改动聚焦于当前任务，不顺手重构无关代码，不覆盖或回退用户已有改动。
- 优先修复根因；除非现有抽象确实无法承载需求，否则不要增加新的中间层或重复实现。
- 业务逻辑应位于 ViewModel、Service 或 Core 中，避免放入 XAML code-behind。
- 持久化数据使用稳定枚举名或字符串键，不使用本地化显示文本作为 ID。
- 用户可见文本应同步维护中文和英文资源。
- 捕获会话、目标窗口和输入设备是共享资源。脚本执行与自动任务不得并行控制游戏；UI 操作后必须重新识别状态，不能假设点击必然成功。
- 坐标和识图规则默认以 `1920 x 1080` 为基准。修改相关逻辑时要考虑缩放、语言、窗口比例、重试、取消和失败恢复。
- 不提交 `bin/`、`obj/`、`.vs/`、本地配置、用户脚本、日志或其他生成物。

## Subagent 许可

- 本仓库明确允许主代理使用 subagent，无需为每次委派另行征得用户许可。
- 当任务可拆分为边界清晰、相互独立的调研、实现、测试或审查工作时，可以并行委派给 subagent。
- 不要让多个 subagent 同时修改同一文件或同一段逻辑。委派前应说明范围、预期产物和禁止事项。
- 主代理始终负责最终整合：检查 subagent 的结论与改动、解决冲突、运行验证，并确保最终结果符合本文件和用户要求。
- 对简单、强耦合或无法安全并行的任务，直接由主代理完成，避免无意义拆分。

## 代码与测试

- 使用 C# nullable 约束，避免用 `!`、空 catch 或宽泛异常处理掩盖问题。
- 异步代码应传播 `CancellationToken`；涉及 UI 的状态更新必须遵守 WPF Dispatcher 线程约束。
- 新增或修复非平凡行为时，在 `BetterBTD.Tests/` 中补充与风险相匹配的 xUnit 测试。
- 修改脚本格式、绑定文件、HTTP 协议、配置结构或用户工作流时，同步更新对应文档和兼容性测试。
- 修改 XAML 时保持现有 WPF UI 视觉与交互规范，并检查中英文文本、不同窗口尺寸和主题下的布局。
- 修改 OCR、地图或自动任务规则时，除自动化测试外，应说明真实 BTD6 环境中已验证和未验证的范围。

## 验证命令

在 Windows x64 环境中，代码改动完成后默认执行：

```powershell
dotnet restore BetterBTD.slnx
dotnet build BetterBTD.slnx -c Release /p:Platform=x64
dotnet test BetterBTD.slnx -c Release --no-build /p:Platform=x64
git diff --check
git status --short
```

- 开发过程中可先运行相关测试项目或测试类，但提交前应尽量完成上述全量 Release/x64 构建与测试。
- 纯文档改动至少执行 `git diff --check` 并检查链接、命令和事实准确性。
- 无法运行某项验证时，不得声称已通过；应在交付说明中写明原因、已完成的替代检查和剩余风险。

## 提交与推送

- 只要任务产生代码改动，代理就必须在验证和最终差异审查完成后创建 Git 提交，并将当前分支推送到其上游远端；不要把提交或推送留给用户作为默认收尾步骤。
- 提交前使用 `git status --short` 和 `git diff` 确认范围，只暂存本任务相关文件。不得夹带用户原有改动、生成物、密钥或本地配置。
- 提交信息应简洁描述实际目的，并遵循仓库已有风格；不要创建空提交。
- 推送优先使用 SSH 远端；仅在 SSH 不可用时回退到 HTTPS，并在交付说明中注明原因。
- 推送前确认当前分支及其 upstream。使用普通 `git push` 或等价的非破坏性命令；未经用户明确授权，不得 force-push、改写历史、rebase、amend 或删除远端分支/标签。
- 若用户明确要求不提交、不推送或采用其他 Git 流程，遵从用户要求。
- 若提交或推送因权限、认证、分支保护、网络或远端更新失败，保留本地提交，不做破坏性补救，并在最终回复中报告提交哈希、失败命令和阻塞原因。
- 纯文档或调查任务仅在用户要求时提交和推送；但文档与代码属于同一功能改动时，应包含在该功能提交中一并推送。

## 交付说明

- 最终回复应简要说明完成内容、验证结果以及提交和推送状态。
- 提及未验证项、兼容性影响或需要真实游戏环境确认的内容，不隐藏失败和剩余风险。
