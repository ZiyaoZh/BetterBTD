

### 地图区域 ID

| ID | 位置范围 |
|---|---|
| 0 | `120 < Y < 430` 且 `X < 740` |
| 1 | `120 < Y < 430` 且 `740 <= X < 1170` |
| 2 | `120 < Y < 430` 且 `1170 <= X < 1600` |
| 3 | `430 < Y < 750` 且 `X < 740` |
| 4 | `430 < Y < 750` 且 `740 <= X < 1170` |
| 5 | `430 < Y < 750` 且 `1170 <= X < 1600` |

### 徽章坐标与颜色

| 模式 | 坐标 | 颜色数组 |
|---|---:|---|
| Standard Easy | `(406,366)` | `0xA88859` |
| Standard Medium | `(492,366)` | `0xA88859, 0xC37503` |
| Standard Hard | `(580,366)` | `0xA88859, 0xA9B9C4, 0xBE7813` |
| Primary Only | `(382,398)` | `0xB08959` |
| Military Only | `(468,399)` | `0xB08959, 0xC37503` |
| Magic Monkeys Only | `(543,377)` | `0xB08959, 0xA9BCCA, 0xC37503` |
| Deflation | `(436,398)` | `0xB08959` |
| Apopalypse | `(495,398)` | `0xB08959, 0xC37503` |
| Reverse | `(520,398)` | `0xB08959, 0xC37503` |
| Half Cash | `(607,398)` | `0xB08959, 0xA9BCCA, 0xC37503` |
| Double HP MOABs | `(550,398)` | `0xB08959, 0xA9BCCA, 0xC37503` |
| Alternate Bloons Rounds | `(615,377)` | `0xB08959, 0xA9BCCA, 0xC37503` |
| Impoppable | `(666,366)` | `0xA88859, 0xA9B9C4, 0xBE7813` |
| CHIMPS | `(693,398)` | `0xB08959, 0xA9BCCA, 0xC37503` |

注意这里的颜色指的是**徽章未获得时的颜色**，而不是徽章本身的颜色。及若数组中的颜色都不匹配，则说明该徽章已获得。

### 坐标偏移公式

- `deltaX = (mapEreaId % 3) * 423`
- `deltaY = (mapEreaId / 3) * 313`

所以实际取点都是：

> `实际坐标 = 基础坐标 + (deltaX, deltaY)`

---