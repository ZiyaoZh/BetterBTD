<ui:FluentWindow x:Class="BetterBTD.Views.Windows.PickerWindow"
                 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                 xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                 xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                 xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
                 mc:Ignorable="d"
                 Width="760"
                 Height="560"
                 MinWidth="680"
                 MinHeight="460"
                 ShowInTaskbar="False"
                 ExtendsContentIntoTitleBar="True"
                 WindowBackdropType="Mica"
                 WindowCornerPreference="Round"
                 WindowStartupLocation="CenterOwner"
                 PreviewKeyDown="OnPreviewKeyDown">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <ui:TitleBar x:Name="PickerTitleBar"
                     Grid.Row="0"
                     Title="选择捕获窗口">
            <ui:TitleBar.Icon>
                <ui:SymbolIcon Symbol="WindowConsole20" />
            </ui:TitleBar.Icon>
        </ui:TitleBar>

        <Grid Grid.Row="1" Margin="24,16,24,0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <ui:TextBlock Grid.Row="0"
                          Margin="0,0,0,12"
                          Foreground="{ui:ThemeResource TextFillColorTertiaryBrush}"
                          Text="双击窗口可直接确认，也可以先选中再点击“选择”。"
                          TextWrapping="Wrap" />

            <ListView x:Name="WindowList"
                      Grid.Row="1"
                      BorderThickness="0"
                      MouseDoubleClick="WindowList_OnMouseDoubleClick"
                      SelectionMode="Single">
                <ListView.ItemContainerStyle>
                    <Style TargetType="ListViewItem">
                        <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                    </Style>
                </ListView.ItemContainerStyle>
                <ListView.View>
                    <GridView AllowsColumnReorder="False">
                        <GridViewColumn Width="470" Header="窗口">
                            <GridViewColumn.CellTemplate>
                                <DataTemplate>
                                    <Grid Margin="0,2">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="28" />
                                            <ColumnDefinition Width="*" />
                                        </Grid.ColumnDefinitions>

                                        <Border Grid.Column="0"
                                                Width="20"
                                                Height="20"
                                                VerticalAlignment="Center"
                                                Background="{ui:ThemeResource CardBackgroundFillColorDefaultBrush}"
                                                CornerRadius="4">
                                            <Image Width="16"
                                                   Height="16"
                                                   HorizontalAlignment="Center"
                                                   VerticalAlignment="Center"
                                                   Source="{Binding Icon}" />
                                        </Border>

                                        <StackPanel Grid.Column="1" Margin="8,0,0,0">
                                            <TextBlock FontWeight="SemiBold" Text="{Binding Title}" TextTrimming="CharacterEllipsis" />
                                            <TextBlock Foreground="{ui:ThemeResource TextFillColorTertiaryBrush}"
                                                       Text="{Binding DisplayName}"
                                                       TextTrimming="CharacterEllipsis" />
                                        </StackPanel>
                                    </Grid>
                                </DataTemplate>
                            </GridViewColumn.CellTemplate>
                        </GridViewColumn>
                        <GridViewColumn Width="180" Header="进程" DisplayMemberBinding="{Binding ProcessName}" />
                    </GridView>
                </ListView.View>
            </ListView>
        </Grid>

        <StackPanel Grid.Row="2"
                    Margin="24,16,24,20"
                    HorizontalAlignment="Right"
                    Orientation="Horizontal">
            <ui:Button Margin="0,0,8,0"
                       Appearance="Secondary"
                       Click="CancelButton_OnClick"
                       Content="取消" />
            <ui:Button Click="SelectButton_OnClick"
                       Content="选择" />
        </StackPanel>
    </Grid>
</ui:FluentWindow>
